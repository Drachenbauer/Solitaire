extends Node


# Declare member variables here. Examples:
# var a = 2
# var b = "text"


# Called when the node enters the scene tree for the first time.
func _ready():
	#for card in storage_stack:
		#card.show_front()
		
		#if card.suit == SUITS.h:
			#card.position = card.position + Vector2(0, storage_stack.find(card) * 16)
		#elif card.suit == SUITS.d:
			#card.position = card.position + Vector2(card.CARD_SIZE.x, (storage_stack.find(card) - 13) * 16)
		#elif card.suit == SUITS.s:
			#card.position = card.position + Vector2(card.CARD_SIZE.x * 2, (storage_stack.find(card) - 26) * 16)
		#elif card.suit == SUITS.c:
			#card.position = card.position + Vector2(card.CARD_SIZE.x * 3, (storage_stack.find(card) - 39) * 16)
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
